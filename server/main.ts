import { Meteor } from 'meteor/meteor';
import './players.ts';
import './games.ts';
import './gamemasters.ts';
import '../collections/methods.ts';

Meteor.startup(() => {
  // code to run on server at startup
});
